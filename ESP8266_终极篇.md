# ESP8266 OneNet 终极篇 —— 非阻塞改造

## 问题现象

- 触摸屏完全没反应（或极度卡顿，点好几下才有一次响应）
- 串口输出正常，心率测量正常，OneNet 上传也正常（偶尔成功）
- 注释掉 `OneNet_Report_Task()` 后触摸立即恢复丝滑

## 根因分析

**罪魁祸首：`ESP8266_Send_AT_Cmd()` 内部的 `HAL_Delay()`。**

调用链：
```
main while(1)
  → MX_TouchGFX_Process()      // TouchGFX 渲染，每 20ms 调用 sampleTouch() 读触摸
  → OneNet_Report_Task()       // ← 阻塞！
       → ESP8266_Send_AT_Cmd() // 内部 HAL_Delay(100~10000)，卡住整个主循环
```

HAL_Delay 阻塞期间：
- `MX_TouchGFX_Process()` 不被调用 → `sampleTouch()` 不被调用 → 触摸事件丢失
- 帧率从 50Hz 暴跌到 ~5Hz（取决于 AT 命令超时时间）
- 一个 WiFi 连接（`AT+CWJAP`）超时 10 秒 = 10 秒无触摸

## 解决方案

### 核心思路

利用 ESP8266 的 UART 空闲中断接收机制：AT 命令发出后立刻返回，响应由中断自动收入 `Data_RX_BUF`，下一轮主循环再检查是否收到预期应答。

### 改动一：非阻塞 AT 命令 (`bsp_esp8266.c`)

```c
// 发送命令后立即返回，不等待
void ESP8266_NB_AT_Start(const char *cmd, const char *ack1, const char *ack2, uint32_t timeout_ms)
{
    ESP8266_Fram_Record_Struct.InfBit.FramLength = 0;    // 清空接收缓冲
    memset(ESP8266_Fram_Record_Struct.Data_RX_BUF, 0, RX_BUF_MAX_LEN);
    ESP8266_USART("%s\r\n", cmd);                         // 发命令
    // 记录期望应答和超时时间
    strncpy(nb_at_ack1, ack1 ? ack1 : "", ...);
    strncpy(nb_at_ack2, ack2 ? ack2 : "", ...);
    nb_at_deadline = HAL_GetTick() + timeout_ms;
    nb_at_state = 1;  // 进入等待状态
}

// 每次主循环调用，检查应答是否到达
// 返回: 0=还在等, 1=成功, -1=超时/失败
int ESP8266_NB_AT_Poll(void)
{
    if (nb_at_state == 0) return 1;   // 空闲=成功
    // 检查 Data_RX_BUF 中是否有期望的应答字符串
    if (strstr(buf, nb_at_ack1) || strstr(buf, nb_at_ack2))
        { nb_at_state = 0; return 1; }
    // 超时？
    if (HAL_GetTick() >= nb_at_deadline)
        { nb_at_state = 0; return -1; }
    return 0;  // 继续等
}
```

**关键点**：旧函数 `HAL_Delay(time)` 死等 → 新方案每轮主循环花 <1ms 检查一次字符串，不阻塞。

### 改动二：OneNet 状态机重写 (`bsp_esp8266.c`)

原来的 `OneNet_Report_Task()` 用 `loop_counter` 粗略分隔阶段，但每个阶段内部仍调用阻塞函数。重写为 17 个原子的状态：

```
OS_SEND_AT        → 发 "AT"
OS_WAIT_AT        → 等 "OK"
OS_SEND_MODE      → 发 "AT+CWMODE=1"
OS_WAIT_MODE      → 等 "OK"
OS_SEND_WIFI      → 发 "AT+CWJAP=..."
OS_WAIT_WIFI      → 等 "OK"（超时 10s，失败重试）
OS_SEND_CLEAN     → 发 "AT+MQTTCLEAN=0"
OS_WAIT_CLEAN     → 等 "OK"
OS_SEND_CFG       → 发 "AT+MQTTUSERCFG=..."
OS_WAIT_CFG       → 等 "OK"
OS_SEND_CONN      → 先发 "AT" 确认模块正常
OS_WAIT_CONN      → 等 AT OK → 发 "AT+MQTTCONN=..." → 等 OK/ALREADY CONNECTED
OS_CONNECTED      → 每 10s 触发一次上报
OS_PUB_SEND_CMD   → 发 "AT+MQTTPUBRAW=..."
OS_PUB_WAIT_PROMPT→ 等 ">" 提示符
OS_PUB_SEND_DATA  → 发 JSON payload（不带 \r\n）
OS_PUB_WAIT_OK    → 等 "SEND OK" 或 "MQTTPUB:OK"
```

每次调用 `OneNet_Report_Task()` 只执行一个 case，做完立刻 `return`。状态靠 static 变量 `os` 跨调用保持。

### 改动三：恢复调用 (`main.c`)

```c
// 之前临时注释掉
OneNet_Report_Task();  // 已恢复，不再阻塞主循环
```

## 涉及文件

| 文件 | 改动 |
|------|------|
| `Core/Src/bsp_esp8266.c` | 新增 `ESP8266_NB_AT_Start/Poll`；重写 `OneNet_Report_Task()` 状态机 |
| `Core/Inc/bsp_esp8266.h` | 新增两个函数声明 |
| `Core/Src/main.c` | 取消注释 `OneNet_Report_Task()` |

## 调试计数器说明（已确认触摸正常后可清理）

| 变量 | 位置 | 含义 |
|------|------|------|
| `g_touch_irq_cnt` | bsp_ft6336.c | FT6336 触摸中断触发次数（ISR 中递增） |
| `g_sample_touch_calls` | STM32TouchController.cpp | TouchGFX 调用 sampleTouch 的次数 |
| `g_hr_continue_flag` | user_app.c | HR 状态机续传标志，1=下一轮继续处理 |

- `g_sample_touch_calls` / `g_touch_irq_cnt` ≈ 主循环每轮读触摸的效率比
- OneNet 阻塞时这个比值很低（IRQ 大量积压但 sample 来不及处理）
- 修复后两者接近 1:1

## 效果

- 触摸保持 50Hz 响应，滑动丝滑
- OneNet 每 10s 自动上报一次，不丢数据
- 心率测量和触屏互不干扰
