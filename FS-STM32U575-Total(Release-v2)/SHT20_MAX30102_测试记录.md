# SHT20 / MAX30102 元器件测试记录

> 日期：2026-05-05

---

## 关键技巧：绕过 GUI 页面限制直接在串口输出传感器数据

### 背景

项目的传感器数据采集由任务调度器（TIM16 + TIM17）管理，任务使能标志 `gTaskEnMark` 由 TouchGFX 界面控制：

| 传感器 | 读取函数 | 使能标志 | 触发条件 |
|--------|----------|----------|----------|
| SHT20 | `Update_AppPageInfo()` | `UPDATE_APP_TASK_EN` | 仅 ApplicationPage 界面 |
| MAX30102 | `Update_HeartRateInfo()` | `UPDATE_HEART_RATE_EN` | 仅 ApplicationPage 点击按钮 |

**如果不在对应 GUI 页面，任务永远不执行，串口永远没输出。**

### 绕过方法

在 `main.c` 的 `USER CODE BEGIN 2` 区域（外设初始化完成后、主循环前），**直接调用传感器驱动函数**，完全不走任务调度器。

---

## 一、SHT20 温湿度传感器

### 硬件接线

| SHT20 | STM32U575 |
|-------|-----------|
| VCC | 3.3V |
| GND | GND |
| SCL | PB6 (I2C1_SCL) |
| SDA | PB7 (I2C1_SDA) |

> I2C 总线需 4.7kΩ 上拉电阻

### 测试代码（Core/Src/bsp_sht20.c）

```c
#define SHT20_DEBUG_PRINTF
#ifdef SHT20_DEBUG_PRINTF
#include <stdio.h>
#endif

void BSP_SHT20_GetData(void)
{
    // ... 原有读取和转换逻辑 ...
#ifdef SHT20_DEBUG_PRINTF
    printf("[SHT20] Raw: T=0x%04X RH=0x%04X | Temp=%.1fC Hum=%.1f%%\r\n",
           pTem, pHum, gTemRH_Val.Tem, gTemRH_Val.Hum);
#endif
}
```

### 测试结果

```
[SHT20] Raw: T=0x67C0 RH=0x5E86 | Temp=24.4C Hum=40.2%
```

✅ 通过

---

## 二、MAX30102 心率传感器

### 硬件接线

| MAX30102 | STM32U575 |
|----------|-----------|
| VCC | 3.3V |
| GND | GND |
| SCL | PB6 (I2C1_SCL) |
| SDA | PB7 (I2C1_SDA) |
| INT | 不接 |

### 发现的问题 & 解决方案

#### 问题 1：SDA/SCL 接反

- **现象**：`[MAX30102] Init FAILED! Check wiring.`
- **排查**：逐级诊断，Step1 I2C probe 直接失败 → 器件不在总线上
- **根因**：SDA 和 SCL 两根线接反了
- **解决**：交换 SDA/SCL 接线

##### 诊断代码（Core/Src/main.c，USER CODE BEGIN 2 区域）

```c
//=== MAX30102 心率传感器诊断 ===
printf("[SYS] Boot OK, USART1 ready.\r\n");
printf("[MAX30102] Step1: I2C probe addr 0x57...\r\n");
{
    uint8_t dummy = 0x00;
    HAL_StatusTypeDef ret = HAL_I2C_Master_Transmit(&hi2c1, 0xAE, &dummy, 0, 100);
    if(ret == HAL_OK) {
        printf("[MAX30102] Step1 PASS: Device on bus.\r\n");
        // ... 后续步骤 ...
    } else {
        printf("[MAX30102] Step1 FAIL: No device at 0x57! Check SDA/SCL/VCC/GND.\r\n");
    }
}
//=== 诊断结束 ===
```

#### 问题 2：芯片温度读数为 0°C

- **现象**：`[MAX30102] Chip Temp=0.0C`
- **根因**：`max30102.c` 中设置 TEMP_EN 后只等了 1ms，MAX30102 手册要求 ≥29ms
- **解决**：`HAL_Delay(1)` → `HAL_Delay(30)`

```c
// Drivers/MAX30102_Maxim/max30102.c:238
HAL_Delay(30); // 等待温度转换完成 (MAX30102手册要求≥29ms)
```

#### 问题 3：DEBUG_MODE 未开启，串口无心率数据

- **解决**：在 `user_app.c` 顶部加 `#define DEBUG_MODE`

```c
// Core/Src/user_app.c
#define DEBUG_MODE  // 激活所有 #ifdef DEBUG_MODE 的 printf
```

### 最终测试结果

```
W25Q128 OK,flash ID:EF4018
[SYS] Boot OK, USART1 ready.
[MAX30102] Step1 PASS: Device on bus.
[MAX30102] Step2 PASS: Init OK.
[MAX30102] Step3: PART_ID=0x15 (expect 0x15)
[MAX30102] Step4: Chip Temp=31.7C
Temp=31.687500, HR=65, SpO2=100
```

✅ 通过

---

## I2C1 总线设备总览

| 地址 | 设备 | 功能 | 测试结果 |
|------|------|------|----------|
| 0x1E | AP3216C | 环境光/接近 | 已集成 |
| 0x38 | FT6336 | 触摸屏 | 已集成 |
| 0x40 | SHT20 | 温湿度 | ✅ Temp=24.4°C Hum=40.2% |
| 0x57 | MAX30102 | 心率/血氧 | ✅ HR=65, SpO2=100% |
| 0x68 | MPU6050 | 6 轴姿态 | 已集成 |

## 总结

| 元器件 | 接口 | 引脚/地址 | 测试结果 |
|--------|------|-----------|----------|
| SHT20 | I2C1 | 0x40 | ✅ Temp=24.4°C Hum=40.2% |
| MAX30102 | I2C1 | 0x57 | ✅ HR=65, SpO2=100% |

### 永久修复

| 文件 | 修改 | 原因 |
|------|------|------|
| `Drivers/MAX30102_Maxim/max30102.c:238` | `HAL_Delay(1)` → `HAL_Delay(30)` | 温度转换需 ≥29ms |
